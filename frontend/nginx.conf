# server {
#     listen 80;

#     location / {
#         root /usr/share/nginx/html;
#         index index.html;
#         try_files $uri /index.html;
#     }

#     # Serve externally-managed images from the mounted uploads volume.
#     # Production host path : /www/wwwroot/JHS Website/Uploads/images
#     # Mounted inside container at : /usr/share/nginx/uploads
#     # Accessible to the browser as : /Uploads/images/<filename>
#     location /Uploads/images/ {
#         alias /usr/share/nginx/uploads/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#         access_log off;
#     }

#     location /api/ {
#         proxy_pass http://backend:8000/;
#     }
# }


server {
    listen 80;

    client_max_body_size 25m;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /uploads/ {
        alias /usr/share/nginx/uploads/;
    }

    location /Uploads/images/ {
        alias /usr/share/nginx/uploads/;
    }

    location /api/ {
        proxy_pass http://backend:8000/;
    }

    # Website content chatbot — a separate Node.js service (see
    # chatbot-backend/ at the repo root), not the main FastAPI backend above.
    # "chatbot-backend" here is the Docker Compose service name (built from ./chatbot-backend,
    # listening on 3001).
    location /chatbot-api/ {
        resolver 127.0.0.11 valid=30s ipv6=off;
        set $chatbot_upstream "chatbot-backend:3001";
        rewrite ^/chatbot-api/(.*) /api/$1 break;
        proxy_pass http://$chatbot_upstream;
        proxy_buffering off; # needed for the streamed (SSE) chat answers
    }

}